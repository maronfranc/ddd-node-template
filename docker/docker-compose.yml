version: '3.8'

services:
  db-mongodb:
    container_name: ddd-mongodb-container
    image: mongo:6.0.7
    env_file:
      - .env.dev
    volumes:
      - ./container-data/mongodb:/data/mongodb
    ports:
      - "27017:27017"
